global_costmap:
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 2.0
  publish_frequency: 0.5
  static_map: true       # Hector SLAM haritayı sunduğu için burası TRUE olmalı
  
  # Harita dışına çıkmayı engellemek için isteğe bağlı:
  track_unknown_space: true
